# Copie para .env e preencha os valores reais
# cp .env.example .env
# .env NUNCA deve ser commitado no git

# Banco de dados
DB_USERNAME=bazar_user
DB_PASSWORD=TROQUE_ISSO_agora_123

# JWT — gere com: openssl rand -hex 32
JWT_SECRET=GERE_COM_openssl_rand_hex_32

# URL do frontend React em produção (sem barra final)
# Deixe vazio para aceitar apenas localhost em dev
FRONTEND_URL=http://SEU_IP_EC2:3000
